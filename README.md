# FinRecon AI

FinRecon AI is a reconciliation workspace for ingesting payment gateway, bank, and order CSVs, matching transactions, reviewing exceptions, and generating finance reports.

## Run locally

```bash
cd backend && npm install && npm run dev
cd frontend && npm install && npm run dev
```

The frontend runs at `http://localhost:5173` and the API at `http://localhost:4000`.
